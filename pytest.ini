[pytest]
addopts = --strict-markers
markers =
    level1: setup and page smoke checks
    level2: HTML and JavaScript interaction checks
    level3: form validation checks
    level4: GET /hello checks
    level5: POST /login checks

